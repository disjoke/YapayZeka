# server/ klasöründen manuel Web Service oluştururken referans
services:
  - type: web
    name: ekinciler-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
